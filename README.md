# festim-permeation-fit

Permeation flux fitting using FESTIM

## Quickstart

```bash
python -m pip install -e ".[dev]"
pytest
```

## Docs

```bash
python -m pip install -e ".[docs]"
mkdocs serve
```
